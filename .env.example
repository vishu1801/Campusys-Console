VITE_API_BASE_URL=http://localhost:8080

# Comma-separated hostnames the Vite dev server will accept requests for
# (required by Vite when the server is reached via a non-localhost host,
# e.g. through a reverse proxy or a Docker container's --host binding).
ALLOWED_HOSTS=localhost
